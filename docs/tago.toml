title       = "twitch"
baseURL     = "https://twitch-cli.tamnd.com/"
description = "Read public Twitch into structured records. One binary for top live streams, the category directory, a channel with its videos, clips, and schedule, a single video or clip, and search over channels and categories, with no API key and no login."
contentDir  = "content"
outputDir   = "public"
theme       = "tago-doks"
syntaxHighlight = "true"

[params]
github = "https://github.com/tamnd/twitch-cli"
